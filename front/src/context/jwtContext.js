import { createContext } from "react";

export const JwtContext = createContext(); //1.creamos el contexto para que nos rebote por toda la aplicacion
//se lo decimos a través de la app con una variable de estado